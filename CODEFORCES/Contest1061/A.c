#include<stdio.h>

//not done

int main()
{
    long  n, s, counter=0;
    while(scanf("%ld%ld", &n, &s) != EOF)
    {

        while(n--)
        {
            printf("%d", n);
        }
    }

    return 0;
}
