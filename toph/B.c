#include<stdio.h>

int main()
{
    int x, y;
    scanf("%d%d", &x, &y);
    if(x+y >=20)
    {
        printf("Yes! Got it");
    }
    else
    {
        printf("Bad luck Tipu Miah");
    }
    return 0;
}
